// hello
/* syntax test */

// doesn't support multiline comments yet. But maybe eventually!!


/* sup */
;

import foo, bar, baz from './bozoo';
export foo, bar, baz;

"catsss";
'';


[a, b, c].map(\d -> e);

hello_nerd;
[    sup,  nerds ];
(
hello, there)
;[ one, two, three ];

/* hah[] */
[ four, five, six ]; //;

morph something = (\a -> (\b, c -> d))(\e -> f);

[
  seven,
  eight,
  nine

     ];

\_ -> 100.46;

type blah over cat, doc, a;

type blah over cat, doc, a equips
  a: b,
  d: f
;

\cat, dog -> [
  dog,
  (cat, dog)
]();


[];

a.b.c.e().f;
/* */
